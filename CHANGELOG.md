# Changelog

## 1.0.0

- initial public package structure
- `LocalWebView` React Native component
- bundled local asset loading on iOS and Android
- RN <-> native <-> web bridge transport
- `buildLocalWebEntryUrl()` helper
